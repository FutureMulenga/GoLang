package main

import "fmt"


func arrays() {

	numbers := [5]int{1, 2, 3, 4, 5} // Array of integers with a fixed size of 5``

	fmt.Println("Numbers:", numbers, "Length:", len(numbers)) // Printing the array and its length using the len() function
}